# No need to run
load_data.py -> contains class LoadData to load the data
feature_engineering.py -> contains all functions to treat the signal

# Run to see results
predict_walking_poly_forces.py -> prediction of the force when polymander is walking
models_comparison_freq_amp -> plot the 3d metrics of the nine models when modulating the amplitude and frequency

